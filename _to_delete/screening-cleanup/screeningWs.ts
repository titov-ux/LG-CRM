/**
 * Файл намеренно пуст.
 *
 * Здесь жил второй (неиспользуемый) WS-клиент комнаты скрининга — дубль
 * `useScreeningSocket.ts`. Всё, что он умел (questions.updated, hint,
 * hello.lastSeq, graceful `stop`), перенесено в `useScreeningSocket.ts`,
 * который и является единственным клиентом. Не добавляйте сюда код:
 * два WS-клиента на одну ручку неизбежно разъезжаются.
 */
export {};
