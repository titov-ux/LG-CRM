# Шаблон постмортема инцидента

Скопируйте в `docs/incidents/<YYYY-MM-DD>-<короткое-имя>.md` после любого P1/P2-инцидента. Лучше писать «горячим» — в течение 24 часов.

---

## Заголовок

`<YYYY-MM-DD> — <одно предложение, что произошло>`

## Сводка

3–4 предложения для тех, кто не был в курсе. Что сломалось, насколько долго лежало, на кого повлияло.

## Хронология (UTC)

| Время | Что произошло |
|-------|---------------|
| 14:02 | Sentry alert «5xx storm» в Telegram |
| 14:03 | Дежурный (А) подключился |
| 14:05 | Подтверждено — `POST /api/v1/candidates` валится 500 |
| 14:08 | ... |
| 14:21 | Откатили на v1.4.2, ошибки пропали |
| 14:30 | Поставлен фикс в v1.4.4 |

## Impact

- Сколько минут лежало / частично работало.
- Сколько пользователей пострадало (Sentry `affected users`).
- Какие операции деградировали (только candidates? все мутации?).

## Root cause

Что конкретно пошло не так. Не «баг в коде», а «миграция 0008 не накатила default для `scan_status`, поэтому INSERT падал на NOT NULL».

## Что сработало

- Алерт сработал быстро.
- Дежурный был доступен.
- Откат прошёл без потери данных.

## Что не сработало

- Алерт не отличал prod от staging — звонок прилетел всем.
- Не было быстрого канала «выключить эндпоинт без перезапуска».

## Action items

| Что | Owner | Срок | Status |
|---|---|---|---|
| Добавить env-фильтр в Sentry alert | @ops | через неделю | done / todo |
| Завести `nginx return 503` для конкретного path | @backend | — | todo |

## Извлечённые уроки

Что общего, что повторяется. Чего не хватает в runbook'е.
