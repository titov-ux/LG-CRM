// Устарело: AI-подбор теперь живёт прямо внутри AttachCandidateDialog как режим
// «ИИ-подбор» (одна модалка). Содержимое вынесено в MatchCandidatesList.
export { MatchCandidatesList } from './MatchCandidatesList';
