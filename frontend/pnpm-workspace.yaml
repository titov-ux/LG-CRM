# pnpm 10+ требует явного разрешения build-скриптов у зависимостей.
# core-js — постинсталл-баннер с просьбой о донате, билд не нужен.
# esbuild и msw — реальные нативные/служебные билды, нужны для работы.
allowBuilds:
  core-js: false
  esbuild: true
  msw: true
